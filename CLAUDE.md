# CLAUDE.md

## 每次 Turn 必须遵守的铁律

在每个 turn 开始、任何工具调用之前，必须执行以下强制检查：

### 0. 头号铁律：绝对不允许用 python3/bash 扫描文件做无意义验证

`python3 -c "..."` 或 `Bash python3` + `Read` 反复扫描同一个文件来验证同一件事，是死循环的首要原因。**构建成功 = 验证完成**，不需要任何额外的扫描。

### 1. Todo list 检查

**先看 todo list**：如果所有项都是 `completed`，只允许：
- 回复纯文本（如总结当前状态）
- 等待用户新指令
- **绝对禁止调用任何工具**（Bash、Read、Write、Edit、Grep、TodoWrite 等全部禁止）

如果 todo list 有 `pending` 或 `in_progress` 项，才可以调用工具。

### 2. 任务对齐协议

收到新任务后，必须先输出四要素模板与用户对齐：
- **目标**
- **理解**（列出 2-3 种可能的理解让用户选择）
- **验证方式**（用户可操作的检查项，不是脚本扫描）
- **不涉及**
- **步骤上限**

**用户确认后才动手。用户未确认前绝对不写代码。**

### 3. 完成识别

以下信号出现时，任务即终止，立即停止所有工具调用：
- 用户说"达到效果""OK""没问题""可以了"
- 所有 todo list 项变为 `completed`
- 构建成功且用户收到通知

终止后只回复文本总结，不调用任何工具。

### 4. 禁止的重复操作

以下操作属于死循环信号，严格禁止：
- build 成功后又用 python3/bash 扫描文件"确认"
- 反复 Read 同一段代码来"验证"
- TodoWrite 反复标记 completed
- Bash 打印 "ready"/"ok"/"done" 等无意义输出
